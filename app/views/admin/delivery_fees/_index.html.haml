%table{class: 'table table-striped table--responsive table--delivery_fee'}
  %thead
    %tr
      %th 適用状況
      %th ID
      %th= t_model('delivery_fee.per')
      %th= t_model('delivery_fee.fee')
      %th= t_model('delivery_fee.began_at')
      %th 操作
  %tbody
    - delivery_fees.each do |delivery_fee|
      %tr
        %td{'aria-label': '適用状況'}
          %span{class: 'badge badge-info'}= '現在の送料' if delivery_fee.current_delivery_fee?
        %td{'aria-label': 'ID'}= delivery_fee.id
        %td{'aria-label': t_model('delivery_fee.per')}
          - if delivery_fee.current_delivery_fee?
            %span{'data-test': 'delivery-fee__current_per'}= delivery_fee.per
          - else
            = delivery_fee.per
        %td{'aria-label': t_model('delivery_fee.fee')}
          - if delivery_fee.current_delivery_fee?
            %span{'data-test': 'delivery-fee__current_fee'}= delivery_fee.fee
            円
          - else
            = delivery_fee.fee
            円
        %td{'aria-label': t_model('delivery_fee.began_at')}= l(delivery_fee.began_at, format: :long)
        %td{'aria-label': '操作'}
          - unless delivery_fee.began?
            = link_to '編集', edit_admin_delivery_fee_path(delivery_fee), class: 'btn btn-primary'
            = link_to '削除', [:admin, delivery_fee], method: :delete, data: { confirm: t_common('delete_confirm') }, 'data-test': "delivery-fee__destroy_#{delivery_fee.id}", class: 'btn btn-danger'
